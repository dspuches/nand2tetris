class Main {
    /**
     * Main function. Creates game object and runs it.
     */
    function void main() {
        var SnakeGame game;
        let game = SnakeGame.new();

        do game.run();
        do game.dispose();
        return;
    }
}